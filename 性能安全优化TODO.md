# 性能安全优化 TODO

> 最后更新：2026-04-20
> 当前范围：聚焦注册安全与限流优化，暂不展开 Phase 2 实现

---

## 一、Phase 1 注册安全闭环

### 1.1 已完成
- [x] 验证码、答题通过令牌、安全事件的数据模型与迁移落地
- [x] `quiz verify`、`send-code`、`register`、`reset-password` 后端链路改造完成
- [x] 前端注册流程接入服务端答题验证与 `quizToken`
- [x] API 路由限流改为 Redis 优先、内存兜底
- [x] Redis 客户端依赖与环境变量示例已补充
- [x] TypeScript 编译检查通过

### 1.2 当前待完成
- [x] 在本地或测试环境配置 `REDIS_URL` 并验证 Redis 限流实际生效（单测 mock 覆盖）
- [x] 验证 Redis 不可用时可自动回退到内存限流（单测覆盖 client=null + incr 抛异常）
- [x] 验证 `send-code` 邮件发送失败时限流回滚符合预期（集成测试 4 cases）
- [x] 复查 429 返回一致性：发现并修复 `verify-password` 未使用统一限流的问题
- [ ] 将本轮 Redis 限流改动单独提交，保留清晰回滚点

### 1.3 上线前检查
- [ ] 生产环境配置 `REDIS_URL`
- [ ] 生产环境配置 `REDIS_RATE_LIMIT_PREFIX`，避免多项目 key 冲突
- [ ] 确认多实例部署共享同一 Redis
- [ ] 观察 Redis 连接失败日志是否可接受，必要时补降噪策略

---

## 二、后续安全优化储备（本轮不实施）

- [ ] 评估 `middleware.ts` 写入限流迁移到 Edge 兼容 Redis 方案的实现路径
- [ ] 为安全事件增加监控、统计或告警能力
- [ ] 为注册安全链路补自动化测试或最小化回归脚本
- [ ] 梳理 Phase 2 需求并单独拆分任务，不与 Phase 1 混做

---

## 三、性能方向待办

- [ ] 为注册、登录、验证码链路增加耗时埋点
- [ ] 评估验证码、答题校验相关查询的索引与热点路径
- [ ] 梳理图片、静态资源与前端首屏加载优化项
- [ ] 评估高频公开接口的缓存策略与限流阈值是否需要分层
