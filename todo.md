# 社区功能优化 TODO

> 最后更新：2026-04-16

---

## 一、社区互动增强

### 1.1 评论嵌套回复 ✔
- [x] Prisma `Comment` 模型新增 `parentId`、`replyToName` 字段及自关联关系
- [x] 数据库迁移（`prisma migrate` + `prisma generate`）
- [x] 评论创建 API 支持 `parentId` / `replyToName` 参数及父评论校验
- [x] 帖子详情 API 和评论列表 API 返回嵌套结构（顶级评论 + replies）
- [x] 帖子详情页 `community/[id]/page.tsx` 支持回复按钮、嵌套展示、取消回复
- [x] 社区列表页 `community/page.tsx` 的 `CommentSection` 支持嵌套回复

### 1.2 帖子分页 / 无限滚动 ✔
- [x] 社区列表页接入分页 API（`page` + `pageSize` 参数，每页 20 条）
- [x] 使用 `IntersectionObserver` 实现触底自动加载下一页
- [x] 加载中 / 没有更多数据 的状态提示
- [x] 切换话题标签时重置分页状态

### 1.3 帖子排序
- [ ] 支持「热门」排序算法（综合点赞数、评论数、发帖时间加权）
- [ ] 后端 API 增加 `sort=hot|new` 参数

### 1.4 帖子搜索增强
- [ ] 支持按标签、作者筛选
- [ ] 搜索结果高亮关键词

---

## 二、签到

### 2.1 签到打卡可视化 ✔
- [x] 签到 API 返回当月签到日期列表（`checkedDays`）、月度统计（`monthTotal`）
- [x] `CheckinCard` 组件新增月度日历网格，签到日高亮显示
- [x] 签到后即时更新日历状态
- [x] 显示月签到天数和连续天数统计卡片



---

## 三、主题与视觉

### 3.1 暗色模式 ✔
- [x] `tailwind.config.js` 添加 `darkMode: 'class'`
- [x] `globals.css` 添加全面的暗色模式 CSS 覆盖（背景、文字、边框、卡片、输入框等）
- [x] 创建 `ThemeToggle` 组件（太阳/月亮图标切换）
- [x] `Navbar` 桌面端和移动端集成主题切换按钮
- [x] `layout.tsx` 添加防 FOUC 初始化脚本（避免页面加载时闪白）
- [x] 支持 `localStorage` 持久化主题偏好
- [x] 支持跟随系统偏好（`prefers-color-scheme`）

### 3.2 暗色模式 — 粉丝地图适配 ✔
- [x] 粉丝地图页面 `bg-[#fafafa]` 元素添加 `dark:` 适配
- [x] `AmapFanMap` 组件暗色模式下使用 `amap://styles/dark` 底图
- [x] 地图容器、加载态、错误态背景色暗色适配
- [x] 统计卡片、空状态面板、"重置全球视角"按钮暗色适配

### 3.3 暗色模式 — 其他页面适配
- [ ] 活动页面 `events/page.tsx` 暗色模式检查与适配
- [ ] 相册页面 `gallery/page.tsx` 暗色模式检查与适配
- [ ] 个人中心 `me/page.tsx` 暗色模式检查与适配
- [ ] 登录/注册页面暗色模式检查与适配
- [ ] 管理后台暗色模式适配（可选）

---

## 四、性能优化

### 4.1 图片与资源
- [ ] 图片懒加载优化（`loading="lazy"` + 占位骨架屏）
- [ ] 头像/图片加载失败兜底展示
- [ ] 大图预览 Lightbox 体验优化

### 4.2 数据加载
- [ ] 帖子列表骨架屏占位
- [ ] API 请求去重 / 防抖
- [ ] 离线缓存策略（Service Worker / SWR）

---

## 五、用户体验

### 5.1 交互优化
- [ ] 帖子卡片长按/右键菜单（收藏、举报、复制链接）
- [ ] 评论输入框支持 @ 提及用户
- [ ] 图片上传进度条
- [ ] 全局 Toast 通知统一管理

### 5.2 移动端适配
- [ ] 底部 Tab 导航栏（移动端优化）
- [ ] 下拉刷新手势
- [ ] 滑动返回手势支持

---

## 六、SEO 与可访问性

- [ ] 页面 Meta 标签优化（Open Graph / Twitter Card）
- [ ] 结构化数据（JSON-LD）
- [ ] 图片 alt 文本规范
- [ ] 键盘导航与 ARIA 标签

---

## 进度汇总

| 模块 | 状态 |
|------|------|
| 评论嵌套回复 | ✔ 已完成 |
| 帖子无限滚动 | ✔ 已完成 |
| 签到打卡可视化 | ✔ 已完成 |
| 暗色模式（核心） | ✔ 已完成 |
| 暗色模式（粉丝地图） | ✔ 已完成 |
| 暗色模式（其他页面） | 🔲 待完成 |
| 帖子排序算法 | 🔲 待完成 |
| 积分排行榜 | 🔲 待完成 |
| 性能优化 | 🔲 待完成 |
| SEO 优化 | 🔲 待完成 |
